package ru.netology.stats.
        public class StatService {
int sum = 0
        for (long sale : sales) {
                sum += sale;


                }
                return sum

public long avg (sales) / sales.length

public int aboveAvg(long [] sales)
        int count = 0
        for (long sale : sales )
        if (sale>avg)
        count++;
        return count
        }
